# [config]
# [[config.interfaces]]
# # ^wlp:h=static,b=1+uniform,n=1
# regex = '^wlp'
# [[config.interfaces]]
interfaces:
  - regex: "^wlp"
    bool:
      and:
        - hopper: "static"
          band: "2.4GHz"
        - hopper: uniform
          matchesRequired: 1
