data
build

# Private capture and replay artifacts. Sanitized fixtures belong only under
# explicit test fixture directories.
*.pcap
*.pcapng
*.cap
/events.jsonl
/audit-history.jsonl
/host-path*.jsonl
/linktop-captures/

# go build output (the binary itself; main.go is committed)
netwatch

# Cargo (any subtree, e.g. rs/target)
target/
**/target/

# Local agent reports
.agents/
