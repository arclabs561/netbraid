[package]
name = "netmon-replay"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Deterministic replay for experimental Netmon evidence"
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
netmon-evidence = { path = "../netmon-evidence" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10.9"

[dev-dependencies]
tempfile = "3"
